apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: avinash031997-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: vprofile.hkhinfoteck.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: avinash031997-service
            port:
              number: 9090
